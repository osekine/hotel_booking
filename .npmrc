# Retry up to 5 times on network failures (default is 2)
fetch-retries=5
# Wait up to 60s per retry (default is 10s)
fetch-retry-maxtimeout=60000
# Start retry backoff at 1s
fetch-retry-mintimeout=1000
